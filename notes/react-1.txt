- create react app
  .learn about what is main layout component
   .use to share common section acoress multiple pages
    .for example
     .header/footer
- vite
  .vite means quick, vite build to make fast development experience.
- create project in vite
  .run commanad
  .choose project name
  .choose the javascript
  .than do npm install
  .tha do npm run dev / to run the project on local host
- now  create a layout component.
  .make sure that only the others page like home page, contact page rerender but the layout component remain constant.
  .intsall react router dom
  .create browser router
   .create broswer router and config our first browser route
  
  


server side . client side use
question ?
what is rerender?
.When the data changes, React efficiently updates only the parts of your web page that need to change. 
what is useoutlet?
outlet > use to indicate that where the child root of parent should be render.
useOutLet -> use to render the child root of parent root.
react life cycle method?
There are three categories of lifecycle methods: mounting, updating, and unmounting.
the question that asked in random?
what is client side routing?
whats the difference between js and jsx?
.JSX is a JavaScript syntax extension often used with React. JSX allows you to write HTML-like code in your JavaScript files, making it easier to create and    manage user interfaces in React. 

worked on vite js headerBar / sidenav / react-router-dom - layout

useEffect
useState
react life cycle method
class base functional componennt
how the react works?


